class DescriptionStep {
  final String title;
  final String description;
  final bool isActive;

  const DescriptionStep(
      {required this.title, required this.description, this.isActive = true});
}
