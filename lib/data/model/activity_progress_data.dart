class ActivityProgressData {
  final String label;
  final int targetValue;
  final int currentValue;

  const ActivityProgressData({
    required this.label,
    required this.targetValue,
    required this.currentValue,
  });
}
